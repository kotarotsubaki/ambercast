// CLI may import only runtime, so this facade is the required crossing point for the adapter's environment read.
export { readDebugEnvironment } from '#adapters/system/process-debug-environment.js';
